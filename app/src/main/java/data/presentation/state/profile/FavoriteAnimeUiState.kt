package data.presentation.state.profile

import com.google.protobuf.LazyStringArrayList.emptyList
import data.domain.model.profile.FavoriteAnimeModel
import data.domain.model.server.AnimeDetailModel
import data.domain.model.server.CharacterItemModel
import data.presentation.screens.components.AnimeStatus

/*data class FavoriteAnimeUiState(
    val favorites: List<FavoriteAnimeModel> = emptyList(),
    val isLoading: Boolean = false,
    val error: String? = null
)*/
